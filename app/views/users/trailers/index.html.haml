- content_for :pagetitle do 
  Trailers
.grid_8
  = link_to t(:add_trailer), new_users_trailer_path, :class => 'btn btn-info pull-right'
.mws-panel.grid_8
  .mws-panel-header
    %span
      Trailers
  .mws-panel-body.no-padding
    %table.mws-datatable-fn.mws-table
      %thead
        %tr
          %th
          %th= t(:name)
          %th= t(:tractor)
          %th= t(:driver)
          %th
          %th
      %tbody
        - trailers.each do |t|
          %tr
            %td= link_to t(:show), users_trailer_path(t), :class => 'btn btn-small btn-info'
            %td= link_to t.name.titleize, users_trailer_path(t)
            %td
              = link_to t.truck.name.titleize, users_truck_path(t.truck) if t.truck
            %td
              = link_to t.truck.driver.full_name.titleize, users_driver_path(t.truck.driver) if t.truck and t.truck.driver
            %td
              - unless t.photo.blank?
                = image_tag t.photo.thumb.url, :width => '50'
              - else
                = image_tag 'myicons/trailer-icon-small.png', :width => '50'
            %td= link_to 'Edit', edit_users_trailer_path(t), :class => 'btn btn-small btn-info'

